# Generated by CMake (see CLAUDE.md — cache/solution, intermediates, binaries)
/out/
/out-*/
/build/
/bin/

# Packaging inputs/outputs: the Windows-built .exe staged for Linux packaging,
# the bundled Wine tree, and the built packages. Binary artifacts, never committed.
/packaging/payload/
/packaging/wine/
/packaging/dist/

# Local scratch
/error.txt
/msvc.txt
/arm64.txt

# Local guidance file, not part of the repository
/CLAUDE.md
